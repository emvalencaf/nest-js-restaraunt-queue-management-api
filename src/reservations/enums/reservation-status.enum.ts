export enum ReservationStatus {
    PENDING = 'pending',
    CONFIRMED = 'confirmed',
    CHECKEDIN = 'checked-in',
    INUSE = 'in_use',
    CALLED = 'called',
    CANCELLED = 'cancelled',
    DONE = 'done',
}
